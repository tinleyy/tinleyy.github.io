export type indexesInOneChart = {
    data: Array<any>,
    label: Array<any>,
    labelName: Array<any>
}

export type chartOptions = {
    scalesYDisplay: boolean,
    scalesXDisplay: boolean,
    datalabelsDisplay: boolean
}